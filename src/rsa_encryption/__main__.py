"""
RSA Encryption CLI Tool
This module serves as the entry point for the RSA encryption command-line interface (CLI) tool.
It imports the main function from the cli module and executes it when the script is run directly.
"""

from .cli import main


if __name__ == "__main__":
    main()
